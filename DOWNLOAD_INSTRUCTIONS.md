# Инструкция по скачиванию моделей

## ⚠️ Проблема: Недостаточно места на диске

Модели требуют много места:
- **Z-Image-Turbo**: ~12 GB
- **Qwen-Image-Edit-2511**: ~50 GB
- **Всего**: ~62 GB свободного места

## 📥 Способы скачивания моделей

### Способ 1: Автоматическое скачивание (через скрипт)

#### Требования:
- Минимум 62 GB свободного места на диске
- Стабильное интернет-соединение

#### Команды:

```bash
# Скачать все модели
python download_models.py --all

# Скачать только Z-Image-Turbo
python download_models.py --model z-image-turbo

# Скачать только Qwen-Image-Edit
python download_models.py --model qwen-image-edit

# Скачать в другую директорию (если на другом диске больше места)
python download_models.py --all --output-dir D:\models
```

### Способ 2: Ручное скачивание через Hugging Face CLI

#### Установка Hugging Face CLI:

```bash
pip install -U huggingface_hub
```

#### Скачивание моделей:

```bash
# Z-Image-Turbo
huggingface-cli download Tongyi-MAI/Z-Image-Turbo --local-dir ./models/Z-Image-Turbo

# Qwen-Image-Edit-2511
huggingface-cli download Qwen/Qwen-Image-Edit-2511 --local-dir ./models/Qwen-Image-Edit-2511
```

### Способ 3: Скачивание через веб-интерфейс Hugging Face

1. Перейдите на страницы моделей:
   - [Z-Image-Turbo](https://huggingface.co/Tongyi-MAI/Z-Image-Turbo)
   - [Qwen-Image-Edit-2511](https://huggingface.co/Qwen/Qwen-Image-Edit-2511)

2. Нажмите кнопку "Files and versions"
3. Скачайте все файлы вручную
4. Поместите их в соответствующие директории:
   - `./models/Z-Image-Turbo/`
   - `./models/Qwen-Image-Edit-2511/`

### Способ 4: Использование другого диска

Если на системном диске мало места, скачайте модели на другой диск:

```bash
# Например, на диск D:
python download_models.py --all --output-dir D:\ImageGenerator\Models
```

Затем обновите `config.yaml`:

```yaml
models:
  z-image-turbo:
    local_path: "D:/ImageGenerator/Models/z-image-turbo"
  qwen-image-edit:
    local_path: "D:/ImageGenerator/Models/qwen-image-edit"
```

## 🔧 Настройка для использования локальных моделей

После скачивания моделей, обновите `config.yaml`:

```yaml
models:
  z-image-turbo:
    name: "Tongyi-MAI/Z-Image-Turbo"
    local_path: "./models/z-image-turbo"  # Раскомментируйте эту строку
  
  qwen-image-edit:
    name: "Qwen/Qwen-Image-Edit-2511"
    local_path: "./models/qwen-image-edit"  # Раскомментируйте эту строку
```

## ✅ Проверка скачанных моделей

После скачивания проверьте наличие файлов:

```bash
# Проверка Z-Image-Turbo
dir models\z-image-turbo

# Проверка Qwen-Image-Edit
dir models\qwen-image-edit
```

Должны присутствовать файлы:
- `model_index.json`
- `scheduler/`
- `text_encoder/`
- `transformer/`
- `vae/` (для Z-Image-Turbo)
- `tokenizer/` или `processor/`

## 🚀 После скачивания

Модели будут автоматически использоваться локально, если указан `local_path` в конфигурации. Если `local_path` не указан, модели будут загружаться из Hugging Face при первом использовании.

## 💡 Рекомендации

1. **Освободите место на диске**: Удалите ненужные файлы или используйте другой диск
2. **Скачивайте по одной модели**: Если места мало, скачивайте модели по очереди
3. **Используйте стабильное соединение**: Модели большие, прерывание загрузки может потребовать перезапуска
4. **Проверьте место перед началом**: Убедитесь, что есть достаточно места

## 📊 Размеры моделей

| Модель | Параметры | Размер на диске | VRAM (мин) | VRAM (рекомендуется) |
|--------|-----------|-----------------|------------|---------------------|
| Z-Image-Turbo | 6B | ~12 GB | 8 GB | 16 GB |
| Qwen-Image-Edit-2511 | 20B | ~50 GB | 16 GB | 24 GB |
| **Обе модели** | **26B** | **~62 GB** | **16 GB** | **24 GB** |

### Детальная информация:
- **Z-Image-Turbo**: Transformer (~8-9 GB), Text Encoder (~2-3 GB), VAE (~0.5 GB)
- **Qwen-Image-Edit-2511**: Transformer (~40-45 GB), Text Encoder (~6-8 GB)
- **Рекомендуется иметь**: ~70-75 GB свободного места (с запасом)

## 🔄 Возобновление загрузки

Если загрузка прервалась, просто запустите команду снова - скрипт автоматически продолжит с места остановки:

```bash
python download_models.py --all --resume
```

