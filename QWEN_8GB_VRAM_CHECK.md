# Проверка работы Qwen-Image-Edit-2511 на 8GB VRAM

## 🎯 Цель

Проверка правильности работы модели Qwen-Image-Edit-2511 на NVIDIA RTX 4060 с 8GB VRAM, использование ресурсов и эффективность работы.

## ✅ Что проверяется

### 1. Конфигурация для 8GB VRAM

**Обязательные настройки:**
- ✅ `enable_cpu_offload: true` - КРИТИЧНО для 8GB VRAM
- ✅ `sequential_offload: true` - экономит память (медленнее, но безопаснее)
- ✅ `torch_dtype: "float16"` - экономия памяти в 2 раза
- ✅ `low_cpu_mem_usage: true` - экономия памяти при загрузке

**Оптимизации (должны быть отключены):**
- ❌ `compile_model: false` - компиляция требует дополнительной памяти
- ❌ `enable_flash_attention: false` - Flash Attention может занимать больше памяти

### 2. Использование памяти

**Ожидаемые значения при правильной работе:**

- **При загрузке модели:**
  - VRAM: 2-4 GB (только активные компоненты)
  - RAM: 4-6 GB (компоненты на CPU offload)
  
- **Во время редактирования:**
  - VRAM: 6-7.5 GB (пиковое использование)
  - RAM: 6-8 GB (компоненты переключаются между CPU/GPU)
  
- **После редактирования:**
  - VRAM: 2-4 GB (после очистки кэша)
  - RAM: 4-6 GB

### 3. Эффективность работы

**Правильная работа CPU offload:**
- Компоненты модели не все на GPU одновременно
- Во время редактирования компоненты загружаются на GPU по очереди
- GPU используется активно (80-100%) во время шагов генерации
- Нет ошибок "CUDA out of memory"

## 🧪 Запуск теста

### Автоматический тест

```bash
python test_qwen_performance.py
```

Тест автоматически проверит:
1. ✅ Конфигурацию для 8GB VRAM
2. ✅ Загрузку модели
3. ✅ Использование памяти
4. ✅ Редактирование изображения
5. ✅ Эффективность работы

### Ручная проверка

#### 1. Проверка конфигурации

Откройте `config.yaml` и убедитесь:

```yaml
device:
  type: "cuda"
  enable_cpu_offload: true  # ОБЯЗАТЕЛЬНО
  sequential_offload: true   # Рекомендуется

models:
  qwen-image-edit:
    torch_dtype: "float16"  # ОБЯЗАТЕЛЬНО
    low_cpu_mem_usage: true  # ОБЯЗАТЕЛЬНО

optimization:
  compile_model: false  # Должно быть false
  enable_flash_attention: false  # Должно быть false
```

#### 2. Проверка использования GPU

Во время редактирования откройте второй терминал и запустите:

```bash
# Windows PowerShell
nvidia-smi -l 1

# Или Linux
watch -n 1 nvidia-smi
```

**Что должно быть видно:**
- `GPU-Util`: 80-100% во время шагов генерации
- `Memory-Usage`: 6-7.5 GB / 8 GB
- `Power`: увеличивается во время работы

#### 3. Проверка использования памяти

В логах приложения вы увидите:

```
VRAM перед редактированием: X.XX GB выделено, Y.YY GB зарезервировано
VRAM общая: 8.00 GB, свободно: Z.ZZ GB
...
VRAM после редактирования: X.XX GB выделено, Y.YY GB зарезервировано
Использование: XX.X%
```

**Правильные значения:**
- Использование < 85% - отлично
- Использование 85-95% - нормально, но близко к пределу
- Использование > 95% - предупреждение, возможны проблемы

## 🔍 Диагностика проблем

### Проблема: "CUDA out of memory"

**Причины:**
1. CPU offload не включен
2. Sequential offload не включен
3. Другие приложения используют GPU
4. Размер изображения слишком большой

**Решение:**
1. Проверьте `config.yaml` - все настройки должны быть правильными
2. Закройте другие приложения (браузеры, игры, другие ML модели)
3. Уменьшите размер изображения (512x512 вместо 768x768)
4. Перезапустите приложение

### Проблема: GPU не используется (0% utilization)

**Причины:**
1. Модель работает на CPU вместо GPU
2. CPU offload работает неправильно

**Решение:**
1. Проверьте, что `device.type: "cuda"` в config.yaml
2. Проверьте логи - должно быть "CUDA доступна"
3. Убедитесь, что `enable_cpu_offload: true` (не отключает GPU, а оптимизирует использование)

**Важно:** CPU offload НЕ означает, что GPU не используется. GPU используется активно во время вычислений, просто не все компоненты модели находятся на GPU одновременно.

### Проблема: Медленная работа

**Причины:**
1. Sequential offload включен (медленнее, но экономит память)
2. Модель слишком большая для 8GB

**Решение:**
1. Sequential offload - это нормально для 8GB VRAM, это компромисс между скоростью и памятью
2. Если нужна скорость - нужна видеокарта с большим VRAM (16GB+)
3. Можно попробовать отключить sequential_offload, но это рискованно для 8GB

## 📊 Ожидаемые результаты

### При правильной работе:

✅ **Загрузка модели:**
- Время: 30-60 секунд
- VRAM: 2-4 GB
- RAM: 4-6 GB

✅ **Редактирование изображения 512x512:**
- Время: 30-90 секунд (зависит от количества шагов)
- VRAM пик: 6-7.5 GB
- GPU utilization: 80-100% во время шагов
- Без ошибок памяти

✅ **После редактирования:**
- VRAM: 2-4 GB (после очистки)
- Готовность к следующему редактированию

## 🚀 Оптимизация производительности

### Если есть запас памяти:

1. **Увеличить размер изображения:**
   - С 512x512 до 768x768 (если VRAM позволяет)
   - Проверяйте использование памяти после каждого увеличения

2. **Отключить sequential offload (осторожно!):**
   ```yaml
   device:
     sequential_offload: false  # Быстрее, но требует больше памяти
   ```
   - Работает только если есть запас VRAM
   - Может вызвать "out of memory" если памяти недостаточно

3. **Увеличить количество шагов:**
   - Больше шагов = лучше качество, но дольше время
   - По умолчанию: 40 шагов

### Если не хватает памяти:

1. **Уменьшить размер изображения:**
   - С 512x512 до 384x384
   - Меньше качество, но меньше памяти

2. **Уменьшить количество шагов:**
   - С 40 до 20-30 шагов
   - Быстрее, но немного хуже качество

3. **Закрыть другие приложения:**
   - Браузеры с открытыми вкладками
   - Другие ML модели
   - Игры

## 📝 Итоговая проверка

После запуска теста вы должны увидеть:

```
✅ Конфигурация проверена
✅ Модель загружена
✅ Использование памяти в допустимых пределах
✅ Редактирование выполнено успешно
✅ Все тесты пройдены успешно!
```

Если все тесты пройдены - модель работает правильно и эффективно использует 8GB VRAM! 🎉

