# Интеграция GGUF модели Qwen-Image-Edit-2511

## ⚠️ Проблема с GGUF форматом

**GGUF модель** (`unsloth/Qwen-Image-Edit-2511-GGUF`) **не может быть загружена через diffusers** напрямую, потому что:

1. В репозитории есть только `.gguf` файлы (qwen-image-edit-2511-Q4_K_M.gguf, Q4_K_S.gguf и т.д.)
2. Нет стандартной структуры diffusers (нет `model_index.json`, `scheduler/`, `text_encoder/` и т.д.)
3. GGUF формат требует специальной загрузки через **ComfyUI-GGUF** или **llama.cpp**

## 📋 Текущее решение

Используется **оригинальная модель** `Qwen/Qwen-Image-Edit-2511` с максимальными оптимизациями для 8GB VRAM:

- ✅ CPU offload (обязательно)
- ✅ Sequential offload (максимальная экономия памяти)
- ✅ Float16 (экономия памяти)
- ✅ Low CPU memory usage

**Результат**: Модель должна работать на RTX 4060 с 8GB VRAM.

## 🔧 Для использования GGUF версии

Для полной поддержки GGUF требуется интеграция **ComfyUI-GGUF**:

### Шаг 1: Установка ComfyUI-GGUF

```bash
# Клонирование ComfyUI (если еще не установлен)
git clone https://github.com/comfyanonymous/ComfyUI.git
cd ComfyUI

# Установка ComfyUI-GGUF расширения
git clone https://github.com/city96/ComfyUI-GGUF.git custom_nodes/ComfyUI-GGUF
```

### Шаг 2: Интеграция в проект

Требуется создать новый класс `QwenImageEditGGUFGenerator`, который будет:

1. Загружать GGUF файл через ComfyUI-GGUF
2. Использовать ComfyUI workflow для редактирования изображений
3. Конвертировать результаты обратно в PIL Image

### Шаг 3: Изменения в архитектуре

- Добавить зависимость от ComfyUI
- Создать новый pipeline для GGUF моделей
- Обновить `ModelFactory` для поддержки GGUF
- Обновить веб-интерфейс для выбора между diffusers и GGUF версиями

## 📊 Сравнение версий

| Параметр | Оригинальная (diffusers) | GGUF (ComfyUI-GGUF) |
|----------|-------------------------|---------------------|
| Размер модели | ~40-50 GB | ~12-15 GB (Q4_K_M) |
| Загрузка | Через diffusers | Через ComfyUI-GGUF |
| VRAM (8GB) | 6-7GB (с CPU offload) | 4-5GB (квантованная) |
| Скорость | 1-3 минуты | 1-2 минуты (быстрее) |
| Качество | Высокое | Высокое (с квантованием) |
| Сложность интеграции | ✅ Уже интегрировано | ❌ Требует ComfyUI |

## ✅ Рекомендация

**Для текущего проекта**: Используйте оригинальную модель с оптимизациями. Она уже работает и оптимизирована для 8GB VRAM.

**Для будущего**: Если нужна максимальная экономия памяти и скорость, можно интегрировать ComfyUI-GGUF, но это потребует значительных изменений в архитектуре.

## 🔗 Ссылки

- [GGUF модель на Hugging Face](https://huggingface.co/unsloth/Qwen-Image-Edit-2511-GGUF)
- [ComfyUI-GGUF на GitHub](https://github.com/city96/ComfyUI-GGUF)
- [Оригинальная модель Qwen-Image-Edit-2511](https://huggingface.co/Qwen/Qwen-Image-Edit-2511)

