# Использование GGUF модели Qwen-Image-Edit-2511

## 📥 Скачивание GGUF модели

Для скачивания GGUF модели используйте скрипт:

```bash
python download_gguf_model.py
```

Или укажите конкретный файл:

```bash
python download_gguf_model.py qwen-image-edit-2511-Q4_K_S.gguf
```

### Доступные варианты квантования:

- **Q4_K_S** - 12.3 GB (рекомендуется для 8GB VRAM)
- **Q4_K_M** - 13.1 GB (лучшее качество для 8GB VRAM) ⭐
- **Q5_K_M** - 15.0 GB (высокое качество, может не поместиться)

## ⚠️ Важное ограничение

**GGUF формат не может быть загружен через diffusers напрямую.**

Текущая система использует `diffusers`, который не поддерживает GGUF формат. Для использования GGUF модели требуется:

1. **ComfyUI-GGUF** - специальное расширение для ComfyUI
2. Интеграция в архитектуру проекта
3. Создание нового pipeline для GGUF моделей

## ✅ Текущее решение

Используется **оригинальная модель** `Qwen/Qwen-Image-Edit-2511` с оптимизациями:

- ✅ CPU offload (обязательно)
- ✅ Sequential offload (максимальная экономия памяти)
- ✅ Float16 (экономия памяти)
- ✅ Low CPU memory usage

**Результат**: Модель работает на RTX 4060 с 8GB VRAM.

## 🔮 Будущая интеграция GGUF

Для полной поддержки GGUF потребуется:

1. Установка ComfyUI и ComfyUI-GGUF
2. Создание класса `QwenImageEditGGUFGenerator`
3. Интеграция в `ModelFactory`
4. Обновление веб-интерфейса

Подробности в файле `GGUF_INTEGRATION_NOTES.md`.

## 📊 Сравнение

| Параметр | Оригинальная (diffusers) | GGUF (ComfyUI-GGUF) |
|----------|-------------------------|---------------------|
| Размер | ~40-50 GB | ~12-15 GB (Q4_K_M) |
| VRAM (8GB) | 6-7GB (с CPU offload) | 4-5GB (квантованная) |
| Скорость | 1-3 минуты | 1-2 минуты |
| Статус | ✅ Работает | ⏳ Требует интеграции |

## 🚀 Рекомендация

**Сейчас**: Используйте оригинальную модель с оптимизациями - она уже работает и оптимизирована для 8GB VRAM.

**В будущем**: Если нужна максимальная экономия памяти, можно интегрировать ComfyUI-GGUF.

