# Поддержка генерации изображений через Qwen-Image-Edit-2511

## ✅ Что было добавлено

Модель Qwen-Image-Edit-2511 теперь поддерживает **генерацию изображений с нуля**, а не только редактирование существующих изображений.

### Изменения:

1. **Добавлен метод `generate()` в `QwenImageEditGenerator`**:
   - Генерация изображений по текстовому промпту
   - Поддержка всех параметров генерации (размер, шаги, guidance_scale и т.д.)
   - Мониторинг использования VRAM
   - Оптимизация для 8GB VRAM

2. **Обновлен UI (веб-интерфейс)**:
   - Можно выбрать Qwen-Image-Edit-2511 для генерации
   - Автоматическая настройка параметров при смене модели
   - Правильные значения по умолчанию для каждой модели

3. **Улучшена обработка параметров**:
   - Автоматическая коррекция `guidance_scale` для Qwen (1.0 вместо 0.0)
   - Оптимальные параметры по умолчанию для каждой модели

## 🎨 Использование

### В веб-интерфейсе:

1. Откройте вкладку **"🎨 Генерация изображений"**
2. Выберите модель: **Qwen-Image-Edit-2511** (или Z-Image-Turbo)
3. Введите промпт и настройте параметры
4. Нажмите **"🎨 Сгенерировать"**

### Параметры для Qwen:

- **Размер**: 512x512 (рекомендуется для 8GB VRAM)
- **Шаги**: 40 (больше шагов = лучше качество, но дольше)
- **Guidance Scale**: 1.0 (автоматически устанавливается)
- **Seed**: -1 для случайного или любое число для воспроизводимости

### Параметры для Z-Image-Turbo:

- **Размер**: 384x384 (или больше, если позволяет VRAM)
- **Шаги**: 9 (Turbo модели быстрые)
- **Guidance Scale**: 0.0 (для Turbo моделей)
- **Seed**: -1 для случайного

## 📊 Сравнение моделей

| Параметр | Z-Image-Turbo | Qwen-Image-Edit-2511 |
|----------|---------------|----------------------|
| **Скорость** | ⚡ Очень быстро (9 шагов) | 🐢 Медленнее (40 шагов) |
| **Качество** | ✅ Хорошее | ⭐ Отличное |
| **VRAM** | ~6-7 GB | ~6-7.5 GB (с CPU offload) |
| **Время генерации** | 5-15 секунд | 30-90 секунд |
| **Генерация** | ✅ Да | ✅ Да |
| **Редактирование** | ❌ Нет | ✅ Да |

## 🔧 Технические детали

### Как работает генерация в Qwen:

Qwen-Image-Edit-2511 использует тот же pipeline для генерации и редактирования. Для генерации с нуля передается **пустой список изображений** (`image: []`), что означает "создать новое изображение из промпта".

```python
inputs = {
    "image": [],  # Пустой список = генерация с нуля
    "prompt": "Your prompt here",
    "num_inference_steps": 40,
    "guidance_scale": 1.0,
    "true_cfg_scale": 4.0,
    ...
}
```

### Автоматическая настройка параметров:

При выборе модели в UI автоматически обновляются:
- Размер изображения (512x512 для Qwen, 384x384 для Z-Image-Turbo)
- Количество шагов (40 для Qwen, 9 для Z-Image-Turbo)
- Guidance Scale (1.0 для Qwen, 0.0 для Z-Image-Turbo)

## 💡 Рекомендации

### Когда использовать Qwen для генерации:

- ✅ Нужно высокое качество изображения
- ✅ Есть время подождать (30-90 секунд)
- ✅ Нужна детализация и точность

### Когда использовать Z-Image-Turbo:

- ✅ Нужна быстрая генерация (5-15 секунд)
- ✅ Достаточно хорошего качества
- ✅ Нужно сгенерировать много изображений

## 🐛 Решение проблем

### Ошибка: "CUDA out of memory"

**Решение:**
1. Убедитесь, что `enable_cpu_offload: true` в config.yaml
2. Уменьшите размер изображения (512x512 → 384x384)
3. Уменьшите количество шагов (40 → 30)
4. Закройте другие приложения, использующие GPU

### Ошибка: "Модель не поддерживает генерацию"

**Решение:**
1. Убедитесь, что модель загружена: нажмите "🔄 Загрузить модель"
2. Проверьте, что выбран правильный тип модели
3. Перезапустите приложение

### Медленная генерация

**Это нормально для Qwen!**
- Qwen требует 40 шагов (vs 9 для Z-Image-Turbo)
- Это компромисс между качеством и скоростью
- Для быстрой генерации используйте Z-Image-Turbo

## 📝 Примеры использования

### Генерация через Python:

```python
from generator import ZImageGenerator

# Создание генератора с Qwen
generator = ZImageGenerator(model_type='qwen-image-edit')

# Генерация изображения
image = generator.generate(
    prompt="A beautiful landscape with mountains and lake, sunset, highly detailed",
    height=512,
    width=512,
    num_inference_steps=40,
    guidance_scale=1.0
)

image.save("generated_image.png")
```

### Генерация через CLI:

```bash
# Пока не поддерживается напрямую, но можно использовать через Python скрипт
python -c "
from generator import ZImageGenerator
gen = ZImageGenerator(model_type='qwen-image-edit')
img = gen.generate(prompt='Your prompt here')
img.save('output.png')
"
```

## ✅ Итог

Теперь у вас есть **две модели для генерации**:
- **Z-Image-Turbo** - быстрая генерация
- **Qwen-Image-Edit-2511** - высокое качество + редактирование

Выбирайте модель в зависимости от ваших потребностей! 🎨

