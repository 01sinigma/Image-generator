# Проверка конфигурации Z-Image-Turbo

## ✅ Проверка соответствия официальной документации

### Основные настройки

#### 1. Загрузка модели
- ✅ **torch_dtype**: `bfloat16` (соответствует документации - оптимальная производительность)
- ✅ **low_cpu_mem_usage**: `false` (соответствует документации)
- ✅ **CPU Offload**: Включен через `pipe.enable_model_cpu_offload()` (необходимо для 8GB VRAM)
- ✅ **Sequential Offload**: Включен (`sequential_offload: true`) для экономии памяти на 8GB VRAM

#### 2. Параметры генерации
- ✅ **num_inference_steps**: `9` (результат в 8 DiT forwards для Turbo моделей)
- ✅ **guidance_scale**: `0.0` (должен быть 0 для Turbo моделей)
- ✅ **Размер изображения**: `384x384` (уменьшено для 8GB VRAM, можно увеличить до 512/768)

#### 3. Оптимизации
- ✅ **Attention Backend**: `sdpa` (по умолчанию в diffusers, стабильно)
- ✅ **Flash Attention**: Отключен (экономия памяти)
- ✅ **Model Compilation**: Отключена (экономия памяти)

### 🔧 Исправления внесенные

1. **Загрузка с torch_dtype**: Добавлена попытка загрузки модели с `torch_dtype=bfloat16` согласно документации
2. **Sequential CPU Offload**: Включен для лучшей экономии памяти на 8GB VRAM
3. **Улучшенное логирование памяти**: Добавлена информация об общей и свободной памяти GPU
4. **Переменная окружения**: Создан `start_app.bat` с `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` для оптимизации управления памятью

### 📋 Рекомендации по использованию

#### Запуск приложения

**Вариант 1 (рекомендуется)**: Использовать `start_app.bat`
```batch
start_app.bat
```

**Вариант 2**: Вручную установить переменную окружения
```batch
set PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
python app.py
```

**Вариант 3**: В PowerShell
```powershell
$env:PYTORCH_CUDA_ALLOC_CONF="expandable_segments:True"
python app.py
```

#### Мониторинг памяти

Во время работы можно мониторить использование GPU:
```batch
nvidia-smi -l 1
```

Ожидаемые показатели:
- **GPU-Util**: 80-100% во время шагов генерации
- **Memory-Usage**: 6-7GB / 8GB
- **Power**: увеличится во время генерации

### ⚠️ Известные проблемы и решения

#### Проблема: CUDA out of memory

**Причина**: Модель пытается использовать больше памяти, чем доступно на GPU

**Решения**:
1. ✅ Использовать `sequential_offload: true` (уже включено)
2. ✅ Уменьшить размер изображения до 384x384 (уже установлено)
3. ✅ Использовать переменную окружения `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`
4. ✅ Убедиться, что другие приложения не используют GPU

#### Проблема: VRAM показывает 0.00GB после загрузки

**Причина**: При CPU offload память может не показываться через `torch.cuda.memory_allocated()`, так как компоненты модели хранятся в RAM и загружаются на GPU только во время вычислений.

**Решение**: Это нормальное поведение при CPU offload. Память будет использоваться во время генерации.

### 📊 Текущая конфигурация

```yaml
models:
  z-image-turbo:
    torch_dtype: "bfloat16"  # Оптимальная производительность
    low_cpu_mem_usage: false  # Согласно документации

device:
  type: "cuda"
  enable_cpu_offload: true  # Необходимо для 8GB VRAM
  sequential_offload: true  # Экономия памяти

generation:
  default_height: 384  # Можно увеличить до 512/768
  default_width: 384
  default_num_inference_steps: 9
  default_guidance_scale: 0.0
```

### ✅ Итоговая проверка

- ✅ Конфигурация соответствует официальной документации Z-Image-Turbo
- ✅ Настройки оптимизированы для RTX 4060 с 8GB VRAM
- ✅ CPU offload включен для экономии памяти
- ✅ Sequential offload включен для максимальной экономии памяти
- ✅ Добавлена переменная окружения для оптимизации управления памятью CUDA
- ✅ Улучшено логирование для диагностики проблем

### 🚀 Следующие шаги

1. Запустить приложение через `start_app.bat`
2. Проверить генерацию изображения 384x384
3. Если работает стабильно, можно попробовать увеличить размер до 512x512
4. Мониторить использование памяти через `nvidia-smi`

